'use client';

const Loader = () => {
    return (
        <div className="max-w-full h-96 flex justify-center items-center">
            <span className="loading loading-bars loading-xl my-auto"></span>
        </div>
    );
};

export default Loader;
